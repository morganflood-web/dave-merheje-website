DATABASE_URL="file:./prisma/dev.db"
ADMIN_PASSWORD="your-secure-password"

# Production on Vercel: use a SQLite-compatible remote URL (e.g. Turso / libSQL).
# DATABASE_URL="libsql://...."
# Also set ADMIN_PASSWORD in the Vercel project env vars.
# Image uploads to /public/uploads only work on persistent disk — use Vercel Blob
# or another host for production covers, or commit images under /public/images.
